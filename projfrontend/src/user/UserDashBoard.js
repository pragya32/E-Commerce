import React from "react";
import Base from "../core/Base";
const UserDash = () => {
  return (
    <Base title="UserDash Page">
      <h1>This is user page</h1>
    </Base>
  );
};
export default UserDash;
